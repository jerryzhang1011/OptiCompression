#include <iostream>
#include <vector>
#include <queue>
#include <unordered_map>
#include <sstream>
#include <string>
#include <fstream>

// encode 
std::string buildHuffmanTree(const std::string &text);

// decode
std::string readAndDecode(std::istream& s);

