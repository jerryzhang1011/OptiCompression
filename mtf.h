#include <string>
#include <vector>
#include <iostream>
#include <sstream>
#include <map>
#include <fstream>
#include <stdexcept>


std::string mtf_encoding(const std::string& text);
std::string convert_int_str(int num);
std::string mtf_decode(const std::string& code);